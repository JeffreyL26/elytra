# Reale Broker-Antwort: Yasni (Juni 2026)

Erster realer Datenpunkt für die Opt-Out-Pipeline. Auslöser für
Phase 3b.4.6 (Attachment-Handling + Kategorie `no_data_held`).

Alle personenbezogenen Angaben sind anonymisiert (Max Mustermann,
Musterstraße 1, 12345 Musterstadt, betroffene.person@example.org,
"Sachbearbeiter/in"). Yasni als Firma ist genannt — öffentliche Fakten
aus Impressum/Datenschutzerklärung.

## Timeline

| Zeitpunkt | Ereignis |
|---|---|
| 16.06.2026, 08:58 | Manuelle Anfrage gemäß Art. 15/17/21 DSGVO an Yasni (im Namen der betroffenen Person, mit Vollmacht) |
| 27.06.2026, 12:06 | Antwort von Yasni |

**Antwortzeit: 11 Tage** (innerhalb der Monatsfrist des Art. 12 Abs. 3 DSGVO).

## Kanal-Beobachtungen

- Antwort kam von **`info@`** statt von der angeschriebenen
  Datenschutz-Adresse → Reply-Matching darf sich nicht auf die
  From-Adresse des Brokers verlassen (Token-Matching bleibt der Hauptweg).
- **Substanz steckt im PDF-Anhang** (~550 KB): Der Mail-Body ist ein
  Zweizeiler ("anbei unsere Antwort als PDF"), die eigentliche
  Datenauskunft liegt vollständig im Anhang. Ohne Attachment-Extraktion
  hätte die LLM-Klassifikation nur den nichtssagenden Body gesehen.
- **Identität ohne Ausweiskopie akzeptiert**: Die Vertretungs-Vollmacht
  genügte, es wurde kein zusätzlicher Identitätsnachweis verlangt.
- **Kein Formular-Verweis**: Yasni hat die E-Mail-Anfrage direkt
  bearbeitet, ohne auf ein Web-Formular umzuleiten.

## Inhalt (sinngemäß, anonymisiert)

Mail-Body: kurzer Begleittext, Verweis auf das angehängte PDF.

PDF-Kernaussagen:

1. Zur betroffenen Person liegen **keine Suchergebnisse und kein Exposé**
   vor; die Person ist im Suchindex nicht auffindbar.
2. Verarbeitet werden lediglich Name, IP-Adresse des Absenders und die
   Daten der Korrespondenz selbst — ausschließlich zur Bearbeitung und
   Dokumentation des Ersuchens (Art. 6 Abs. 1 lit. c/f DSGVO), Löschung
   nach Ablauf gesetzlicher Fristen.
3. Das Ersuchen wird als vollständig beantwortet betrachtet.

Der vollständige anonymisierte Wortlaut liegt als Test-Fixture in
`src/lib/llm/__fixtures__/yasni-no-data-held.ts`.

## Soll-Klassifikation

**`no_data_held`, hohe Confidence.**

Begründung: Der Broker bestätigt, dass keine relevanten personenbezogenen
Daten vorliegen — die Anfrage ist vollständig beantwortet. Die erwähnten
Rest-Daten (IP, Korrespondenz) ändern daran nichts (Verarbeitung zur
Anfrage-Bearbeitung selbst, keine Broker-Datenbestände). Explizit NICHT
`success` (keine Löschung bestätigt, es gab nichts zu löschen) und NICHT
`rejected` (keine inhaltliche Weigerung).

## Real-API-Validierung (manueller Lauf)

- **Datum:** 04.07.2026
- **Fixture:** `yasni-no-data-held` · **Modell:** `claude-haiku-4-5-20251001` ·
  **Prompt-Version:** `2026-07-04`
- **Ergebnis:** `no_data_held`, Confidence **0.95**, `manual_review` **false**
- **Modell-Begründung** (gekürzt, wörtlich): „…Rest-Daten (Name, IP-Adresse,
  Korrespondenzdaten) ändern an dieser Klassifikation nichts…"

**Zweck — manueller Regressions-Anker:** Vor jeder Prompt-Änderung erneut
laufen lassen und dieselbe Kategorie verifizieren, **bevor** der neue Prompt
echte Broker-Antworten sieht:

```bash
pnpm tsx --env-file=.env src/scripts/test-classify-real-response.ts
```

Bewusst **kein CI-Test** (API-Kosten, externe Abhängigkeit).

## Abgeleitete Pipeline-Änderungen (Phase 3b.4.6)

1. Attachment-Metadaten in der Inbound-Normalisierung; PDF-Text-Extraktion
   (`extract-attachment-text.ts`, pdf-parse, kein OCR) fließt in die
   LLM-Klassifikation ein.
2. Neue Kategorie `no_data_held` (Klassifikation + `process_status`-Enum),
   Terminal-Status wie `success`. Momentaufnahme: Recurring-Re-Checks
   (Phase 3b.8) dürfen Terminal-Status über `next_action_at` wieder
   aufgreifen.
